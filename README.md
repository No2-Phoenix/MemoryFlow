# MemoryFlow

MemoryFlow 是一个 Flutter 记忆故事播放器项目，当前主代码位于 [memoryflow](d:/Projects/MemoryFlow/memoryflow) 目录。

## 运行

```bash
cd memoryflow
flutter pub get
flutter run
```

## 测试

```bash
cd memoryflow
flutter analyze
flutter test
```
